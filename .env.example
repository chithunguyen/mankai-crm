# Google OAuth Client ID (loại "Web application"). Không phải bí mật — có thể public.
VITE_GOOGLE_CLIENT_ID=
# ID file Google Sheet mặc định (đoạn giữa /d/ và /edit trong link). Người dùng vẫn đổi được trên giao diện.
VITE_SHEET_ID=
